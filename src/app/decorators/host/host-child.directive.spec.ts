import { HostChildDirective } from './host-child.directive';

describe('HostChildDirective', () => {
  it('should create an instance', () => {
    const directive = new HostChildDirective();
    expect(directive).toBeTruthy();
  });
});
