import { HostChildListnerDirective } from './host-child-listner.directive';

describe('HostChildListnerDirective', () => {
  it('should create an instance', () => {
    const directive = new HostChildListnerDirective();
    expect(directive).toBeTruthy();
  });
});
